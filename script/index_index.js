/*var desktop = $("#desktop");

$.each(App.menu, function(index, menu){
    var li = $("<li />");
    var link = $("<a />");

    if(App.control == menu.control) li.addClass="active";

    link.attr("href",menu.control);
    link.append("<i class='glyphicon glyphicon-"+menu.icono+"'></i><span>"+menu.menu+"</span>");

    li.append(link);
    desktop.append(li);
});
*/